<!-- PRISM SESSION HANDOFF — v5.0 ADVERSARIAL STRESS-TEST CAMPAIGN — read this FIRST next session -->
# v5.0 — "make it suffer until it's bulletproof" — next-session handoff (2026-06-01)

**Mission:** adversarially stress-test PRISM until it's release-grade. The first pass (this session) already found a critical deadlock + 8 more real issues and fixed the substantive ones. Your job: **restart, re-verify the fixes hold LIVE, close the restart-gated items, then escalate — torture every surface (the §6 battery) and fix whatever breaks.** Goal = bulletproof for release. Be adversarial; assume it's still hiding bugs.

Prior verdict: core architecture A−, "as-shipped-before-fixes" C+, "after this session" B+ climbing. Don't trust the climb until §4 is closed and §6 finds nothing new.

Companion docs: **`docs/prism/2026-06-01-v5.0-stress-test-report.md`** (the full findings + fixes, **now Rounds 1–8**) and the v5.0 delivery handoff `2026-06-01-v5.0-review-and-delivery-handoff.md`.

---

## ═══ RESUME HERE — Session 3 (ONLY §6.C remains — needs `notebooklm login` + this restart) ═══

**Session 2 is DONE (2026-06-01).** It re-verified Session-1's fixes LIVE, closed the two deferred §4 CODE fixes, and closed slice **F**. See report **Round 9**. The ONLY open battery slice is **C** (agent/skill create→reuse), which is blocked on (a) a NotebookLM **re-auth** and (b) a restart — neither a PRISM defect.

**State at Session-2 end:**
- Branch `feat/v5.0`, **UNCOMMITTED** unless Session 2 already committed (check `git log`). Repo AND live `~/.claude` carry all fixes (the 5 Session-2 files were synced live).
- **Suite green** (23 state test files + NEW `test-prism-safety-overfire` 15/15), `/prism-audit-runner` **29/29**. Re-run §7 to confirm.
- **10 fixes total this campaign**, all TDD'd where a test surface exists.

**DONE in Session 2 (Round 9):**
- **§4#4 safety over-fire** ✅ FIXED + TDD'd (`test-prism-safety-overfire` 15/15). De-quote shell-danger scan + eval-wrapper carve-out + SQL stays raw + dropped `~` from the rm-path rule. `hooks/prism-safety.mjs`.
- **§4#5 PATH-check sweep** ✅ — execution-detection across `dependency-manifest.md`, `prism-health.md:67`, `prism-doctor.md:40`, `hooks/lib/prism-exec.sh` (fallback paths; pinned hot-path intentionally `-x`-only). `install.sh`/`uninstall.sh` left (core-req gates).
- **§6.F productivity A/B** ✅ — real `note`-field feature on the coffee app (app suite 24/24); friction quantified = ~1 forced dispatch + ~1s router/turn, **0 spurious safety blocks** (down from 2; Fix-9 payoff). 1 new INFO finding: routine feature request still under-tiers to haiku (self-override is the net).
- **Baseline re-verify** ✅ — SessionStart `hookEventName` symptom confirmed GONE live; suite + audit green before any change.

**NEXT (Session 3) — finish §6.C, then ship:**
1. **Re-auth NotebookLM first:** run `notebooklm login` in your shell (interactive OAuth; the campaign cannot automate it). Confirm with `python -m notebooklm list`.
2. **§6.C create→reuse (the agent-factory fixes are now live):** create an agent via FREE Tier-1 (`python -m notebooklm`, §3) → confirm **$0 + real `notebooklm_notebook_id` + non-`$0.00` cost**; **restart** → confirm it's dispatchable + REUSE it. Then skill create→use→reuse post-restart.
3. **Faithful 7-phase `/prism-bootstrap`** in a session **cd'd INTO `prism-stress-test/`** (foreign project — prism_3 can't bootstrap itself).
4. **Then ship:** ensure the grouped fix set is committed, re-run full suite + audit, and (human-gated) TASK 5 merge `feat/v5.0`→main, tag `v5.0.0`.

**PENDING DESIGN DECISION (not urgent, do NOT build unprompted): F4 index sharding.** The index rebuild is O(N_total) (write-amplification; ~4.2s merge at 2.2k docs). Evaluated Graphiti (rejected: Python+graph-DB daemon+API keys) and obsidian-second-brain (rejected: no index, pushes O(N) into context). Chosen = dep-free SHARDING by project_id (per-shard file + manifest → O(project) writes). **Trigger: build only when the >2000 warn actually trips.** Full rationale: report "Pending decision — index scaling" + §5 below.

**Still-open finding (by-design, no fix planned):** Windows `claude -p` rerank ~16–19s even pre-warmed > 8s default budget → silent BM25 fallback. Mitigation documented (`--no-rerank` / raise `PRISM_RECALL_RERANK_TIMEOUT_MS`). Not a regression.

---

## 0. Re-verify before trusting anything below ([[feedback-handoff-backlog-reverify]])
- **Branch `feat/v5.0`, ~25 modified+new files, UNCOMMITTED, ahead 2** (the 2 are prior local doc commits). `git status -sb` + `git log --oneline -3` to confirm.
- **Full suite GREEN at handoff** (re-run it — see §7): 19 state suites all pass (incl. 4 NEW test files this session = 18 new tests), `/prism-audit-full` **29/29**.
- **This session's fixes are SYNCED to the live install but most take effect only AFTER restart** (hooks/agents/skills load at process start). So your fresh session is the FIRST real test of them live.

## 1. What this session already FIXED — DO NOT redo (verify they hold)
All TDD'd where a test surface existed; all synced to `~/.claude`. Detail in report §"Fixes applied" + Rounds 2/3:
1. **🔴 Panel-summon deadlock (publish-blocker).** Guards now allow Write/Edit to `.prism-turn-tier-*` override file; router never carries `summon_panel` on continuation-inherit, never inherits on empty/notification turns. Test: `test-prism-panel-deadlock` 5/5. Files: `hooks/prism-parent-dispatch-guard.mjs`, `prism-mutation-guard.mjs`, `prism-prompt-tier-router.mjs`. **PROVEN LIVE** at end of this session (used the override-file Write to escape the mutation-guard).
2. **🟠 Telemetry aggregator** counts real schema (`source`/`event`+`blocked`/`summon_panel`). `test-prism-telemetry-buckets` 3/3. `tools/prism-telemetry-aggregate.mjs`.
3. **🟠 Audit catalog re-baselined** (16/29→29/29) + runner kill-timeout decoupled from latency budget (12s floor) + `setup_sentinel` seeding for stateful guards. `tools/prism-audit-runner.mjs`, `tests/v3/audit-scenarios.json`.
4. **🟡 Sonnet tier revived** (was 0/623). `test-prism-sonnet-routing` 5/5. `tools/lib/prism-tier-classify.mjs`.
5. **🟢 Safety gate** blocks `curl|wget … | bash/sh/python`. audit `SAF-002` green. `hooks/prism-safety.mjs`.
6. **🟡 `ERROR: ERROR:` leak** fixed + Tier-1 index error demoted on `--cross-project`. `test-prism-recall-error-leak` 5/5. `tools/prism-recall.mjs`.
7. **🟠 Silent Opus fallback + `$0.00` cost lie** → agent-factory returns a non-silent NotebookLM-install recommendation; orchestrator PHASE 0 offers the install (AskUserQuestion) before spawning the factory; Tier 2/3 must record real cost. `agents/agent-factory.md`, `skills/master-orchestrator/references/phase-0-team-assembly.md`.
8. **🟠 AppLocker PATH false-positive** in tool detection → detect by EXECUTION (`--version`) + `python -m`, at agent-factory (both flows), `/prism-health`, `/prism-index`. ([[feedback-applocker-exe-detection]])
9. **🟡 Weak research seed-query** → mandatory construction template (domain+methods+tools+context+constraints+failure-mode) in `agents/agent-factory.md`.

## 2. Verified GOOD (don't re-prove unless escalating)
- **Adversarial panel works** — real request: 4 experts, 9 challenges (5 decisive), Phase 1.5 review, caught real code bugs; nonsense request: refused to hallucinate, stopped Phase 0.
- **F4 cross-project recall** bidirectional (prism_3 ↔ prism-stress-test). **Privacy invariants hold** (default-deny, descriptor-not-body, no API key, telemetry opt-in). **Free NotebookLM Tier-1 path** validated end-to-end (create→source→ask→delete).

## 3. ⚠️ Environment notes for next session
- **NotebookLM is INSTALLED + AUTHED** (`notebooklm-py 0.6.0`, account `dedopoulos.diy@gmail.com`, token persists). **MUST invoke as `python -m notebooklm`** — bare `notebooklm.exe` is AppLocker/WDAC-blocked on this PRAKGR domain box. ([[feedback-applocker-exe-detection]])
- **Live PRISM governs the session** ([[prism-live-install-governs-dev-session]]). The deadlock fix means a false panel is now escapable (Write the `.prism-turn-tier-*` override file — confirmed working). If guards still block heavy parent editing: `PRISM_MUTATION_GUARD=off` (+ `PRISM_DISPATCH_GUARD=soft`) + restart, OR dispatch first, OR write the override file with `force_opus:true`.
- **Throwaway substrate exists:** `Y:\Documents\utilities_projects\prism-stress-test\` (Django+DRF coffee-ledger, its own git, bootstrapped with docs/prism + `.prism-kb-share.json`). Reuse it. Agent `coffee-ledger-expert` is in `~/.claude` roster (created via Opus/Tier-3 — recreate via free Tier-1 now to validate that loop).
- **Windows:** subagents use Edit/Write not PowerShell (BOM). `/tmp` mis-resolves in git-bash → pass explicit Windows temp paths for test output.

## 4. MUST-CLOSE before release (restart-gated + deferred)
1. **Validate the deadlock fix LIVE end-to-end** — re-trigger a false panel on a no-prompt/notification turn; confirm it no longer wedges. (Override-file escape already confirmed working this session.)
2. **agent-factory → free Tier-1 FULL LOOP** (now unblockable): recreate `coffee-ledger-expert` (or a new expert) and confirm it uses `python -m notebooklm` Tier-1 ($0), writes a real `notebooklm_notebook_id`, records a non-`$0.00` cost. Then **restart again** and confirm the new agent is dispatchable + REUSE it.
3. **Skill create→use→reuse** post-restart (same registry-at-start gate — confirm a created skill is invocable next session).
4. **Safety over-fire** ✅ DONE (Session 2 / Round 9) — de-quote shell-danger scan + eval-wrapper + SQL-raw carve-outs + dropped `~` from rm-path rule. TDD'd `test-prism-safety-overfire` 15/15. `hooks/prism-safety.mjs`.
5. **Sweep remaining PATH-checks** ✅ DONE (Session 2 / Round 9) — execution-detection across `dependency-manifest.md`, `prism-health.md:67`, `prism-doctor.md:40`, `prism-exec.sh` fallbacks. (`install.sh`/`uninstall.sh` left = core-req gates.)
6. **Final clean full-suite re-run after ALL of the above** + the audit-runner — done each Session-2 fix; re-run again in Session 3 after §6.C closes.

## 5. Outstanding (noted, lower priority)
- Rerank 8s vs ~20s Windows cold-start: documented in `commands/prism-recall.md` (prefer `--no-rerank`); no code fix without a warm-process design.
- Router ~1s/prompt cold-start tax (architectural — persistent classifier process).
- **F4 index O(N_total) write-amplification — PENDING (decision made, not yet built).** Every incremental rebuild re-parses + re-df's + re-serializes the WHOLE single-file index (~4.2s to merge 2nd project at 2.2k docs; grows linearly). Query also O(N) parse+scan but fine to ~50k. **Decision (this session):** evaluated Graphiti (rejected — Python + graph-DB daemon + LLM/embedding API keys; blocked on AppLocker box) and obsidian-second-brain (rejected — no index, pushes O(N) into context window). **Chosen fix = dep-free SHARDING by project_id:** `~/.claude/.prism-kb/shards/<project_id>.json` + `manifest.json` (per-shard df contribution) → rebuild rewrites only the dirty shard (O(project)), global df = sum of shard contributions. Keeps JSON + atomic-write philosophy; no daemon/Python/API. NOT urgent (warn at 2000 is the tripwire; realistic scale far below). Real cost = on-disk FORMAT change + one-time migration + ~5 readers/tests updated. **Trigger: implement when the >2000 warn actually trips.** See report Round 6 + the "Pending: index scaling" note.
- agent-factory Tier-1: prefer lightweight `source add <url>` over `source add-research` deep-research on Windows (the latter 502s).
- **`/prism-retire` unguarded skill-prose — FIXED (§6.C).** Was: model hand-edits `roster.json` (no lock/atomic/validation/traversal-guard). Now: executable `tools/prism-retire.mjs` (parse-before-touch, archive-move not delete, `withRosterLock` + atomic tmp+rename, path-traversal guard on the name); slash command previews `--dry-run` then delegates. TDD'd `test-prism-retire.mjs` 29/29; in install-manifest; synced live. Deterministic uninstall-cleanup also adversarially verified robust (16/16). See report Round 8.

## 6. THE TORTURE BATTERY — "make it suffer" (the actual ask)
Run adversarially on the `prism-stress-test/` substrate; each should survive cleanly OR yield a fixable finding.

**A. Routing / guard chaos**
- Adversarial classifier prompts: try to RE-trigger the deadlock post-fix; make the panel over-fire (ambient release/ship tokens) AND under-fire (a genuine architecture request phrased plainly → does it miss the panel?). Label a 30-prompt set with expected tiers; measure false-pos/neg.
- Concurrency: fire many parallel dispatches → does `prism-dispatch-cap`/`prism-parallel-guard` hold at the cap without deadlock? Race a mutation against a dispatch.
- Chaos/error-injection: corrupt the sentinel / roster.json / a dirty-flag mid-session → does PRISM fail SAFE (allow + warn) or hard-crash?

**B. Privacy / security adversarial (highest stakes)**
- Plant secrets (new SECRET_PATTERNS: Slack/Google/JWT/Stripe/connection-string + originals) in a SHARED project's lesson → rebuild index → confirm NONE reach the global index / `claude -p` egress. Try a keyword-less high-entropy secret (known gap) — does descriptor-not-body still cap the leak?
- Cross-project exfil WITHOUT a `.prism-kb-share.json` (default-deny must hold). Symlink/path-traversal in the corpus walk.

**C. Agent/skill lifecycle at scale**
- Create 2–3 agents via free Tier-1; reuse across restarts; retire one (`/prism-retire`); confirm roster integrity + `/prism-uninstall-cleanup` only touches plugin-tagged.
- Faithful `/prism-bootstrap` in a SEPARATE session cd'd INTO `prism-stress-test/` (the test prism_3 couldn't do) → does the full 7-phase machine work on a real foreign project?

**D. F4 at scale**
- Generate hundreds of lessons/adjudications across ≥2 shared projects → cross the ~2000-doc sharding threshold → does the warn fire? Does BM25 stay sub-second? Measure index build + recall latency at scale. Re-rank with a PRE-WARMED `claude -p` to actually exercise the LLM rerank (not just cold-start BM25 fallback).

**E. Telemetry / calibration**
- Opt in, accrue events, run `--recommend-calibration` + `--tuning` → sensible? Does the rollup now match raw-log reality (post-fix)?

**F. Productivity A/B**
- A representative feature on the coffee app WITH guards on vs `PRISM_*_GUARD=off` → quantify the friction tax (turns, cost, spurious blocks) honestly.

## 7. Verification commands
```bash
cd Y:/Documents/utilities_projects/prism_3
for t in test-prism-panel-deadlock test-prism-telemetry-buckets test-prism-sonnet-routing \
  test-prism-recall-error-leak test-prism-v4-6-classifiers test-scope-guard test-prism-dispatch-cap \
  test-prism-telemetry-consent test-prism-telemetry-v4-5 test-prism-bm25 test-prism-kb-knowledge-indexer \
  test-prism-kb-rerank test-prism-recall-cross-project test-prism-kb-knowledge-rebuild \
  test-prism-kb-autosync-knowledge test-prism-freshness-knowledge test-manifest-coverage \
  test-prism-release-screen-panel test-plugin-manifest-drift \
  test-prism-routing-chaos test-prism-retire test-prism-uninstall-cleanup \
  test-prism-safety-overfire test-oob-pickup-e2e; do node "tests/v3/state/$t.mjs"; done
# NOTE: run sequentially (NOT in a `&&` loop redirecting to /dev/null) — back-to-back
# real-subprocess tests show transient false "FAIL" on Windows ([[feedback-installer-state-test-flaky-windows]]).
node tools/prism-audit-runner.mjs --output "C:/Users/ServosY/AppData/Local/Temp/audit.jsonl"   # expect 29/29
python -m notebooklm list   # confirm NotebookLM auth persisted (NOT bare notebooklm)
```

## 8. Pointers
- Findings + fixes: `docs/prism/2026-06-01-v5.0-stress-test-report.md` (Rounds 1–3).
- Memories: [[project-v5-stress-test-deadlock]], [[feedback-applocker-exe-detection]], [[prism-live-install-governs-dev-session]], [[feedback-claude-p-cold-start-windows]], [[feedback-top-class-quality-bar]].
- After torture battery passes clean + §4 closed: commit the fix set (grouped), then TASK 5 = merge `feat/v5.0`→main, tag `v5.0.0`, push (human-gated). DO NOT merge until bulletproof.

<!-- PRISM SESSION HANDOFF — v5.0 REVIEW + DELIVERY — read this FIRST next session -->
# v5.0 review + delivery — next-session handoff (written 2026-06-01)

**Mission for the next (fresh) session, in order:**
1. **Update the `claude-master` agent to Opus 4.8** (it currently tops out at Opus 4.7 — see §3).
2. **Run a fresh codebase + security + performance review of v5.0** using the updated `claude-master` (see §4).
3. **Triage + fix** review findings (re-run the test sweep after).
4. **Merge `feat/v5.0` → `main`, tag `v5.0.0`, push, deliver** (the human-gated step — see §5).

Companion docs: [`2026-06-01-v5.0-f4-phase-e-handoff.md`](2026-06-01-v5.0-f4-phase-e-handoff.md) (what F4 Phase E built) and the locked design [`../plans/2026-05-29-F4-design.md`](../plans/2026-05-29-F4-design.md).

---

## 0. Re-verify before trusting anything below ([[feedback-handoff-backlog-reverify]])
- **Branch:** `feat/v5.0` @ `41b95ebc4`, **in sync with origin**, working tree clean (verified 2026-06-01).
- **Last 8 commits** (newest first): `41b95ebc4` routing fix · `1d939a03d` prism-recall doc · `d76a3c14e` cut v5.0.0 · `5adb973db` Phase E · `77e8cd031` D · `e4b7e172d` C · `a9836f4c4` B · `f11f17d43` A.
- **Version:** 5.0.0 across manifest + plugin.json; CHANGELOG `[5.0.0]`.
- **Ship-readiness already audited (SHIP, no blockers)** — but the next session's reviews are the deliberate pre-merge gate the human wants, so treat the prior audit as a baseline, not a substitute.

## 1. ⚠️ Two environment gotchas that WILL bite the next session
1. **The routing fix is committed but NOT live.** `hooks/lib/prism-opus-classifier.mjs` has the release-screen→panel decoupling in the **repo** (string "DESIGN PANEL is decoupled" present), but `~/.claude/hooks/lib/prism-opus-classifier.mjs` (the **installed/running** copy) does **NOT** — so the live session still over-fires the panel on "ship/release/review" prompts. To make the fix (and all of v5.0) active in the running session: re-run the installer (`node tools/prism-installer.mjs update`) **and restart the Claude Code session**. Until then, expect panel-summoning on release/review prompts; bypass with `!opus-force:` prefix or `PRISM_DISPATCH_GUARD=off`.
2. **Live PRISM governs the session** ([[prism-live-install-governs-dev-session]]). The mutation-guard + dispatch-guard are active. Updating `claude-master` and fixing review findings = lots of parent Edit/Write — if the guards block them, set `PRISM_MUTATION_GUARD=off` (+ `PRISM_DISPATCH_GUARD=off` for parent tools) and restart, OR work through a dispatch. The installer-state test is **flaky on Windows** ([[feedback-installer-state-test-flaky-windows]]) — a varying failure set ≠ regression.

## 2. Authoritative Opus 4.8 facts (from this session's environment — VERIFY + cite per claude-master doctrine)
The next session must update `claude-master` against current docs (its own doctrine demands code.claude.com citations), but here are the known-true facts to anchor on:
- **Model:** Opus 4.8, **1M context**, model ID **`claude-opus-4-8`** (1M variant `claude-opus-4-8[1m]`).
- **Family:** most recent is Claude 4.X. IDs: Opus 4.8 `claude-opus-4-8` · Sonnet 4.6 `claude-sonnet-4-6` · Haiku 4.5 `claude-haiku-4-5-20251001`.
- **Fast mode:** uses Opus with faster output (NOT a downgrade to a smaller model); toggle `/fast`; available on Opus **4.8 / 4.7 / 4.6**.
- **Effort levels:** `low|medium|high|xhigh|max` + the `ultrathink` keyword (unchanged).
- **Default routing for claude-master's advice:** Haiku 4.5 trivial · Sonnet 4.6 routine · **Opus 4.8 novel** (was 4.7).
- Knowledge cutoff: January 2026.

## 3. claude-master update spec (TASK 1)
- **File:** `agents/claude-master.md` (repo) — **1,392 lines**, frontmatter `model: opus`, `effort: high`, `maxTurns: 40`, `memory: true`. Also installed at `~/.claude/agents/claude-master.md` (update both, or update repo + re-install).
- **It mentions Opus 4.8 NOWHERE; newest reference is Opus 4.7.** Specific lines to update (verified 2026-06-01):
  - **L24–26** (description): "model routing (Opus 4.7 / Sonnet 4.6 / Haiku 4.5 …)" → Opus 4.8.
  - **L75–77**: "Opus 4.7 for novel work … `xhigh` … on Opus 4.7 (already the default)" → Opus 4.8.
  - **L225–227**: full IDs `claude-opus-4-7` → `claude-opus-4-8`; "`opus` = Opus 4.7 (May 2026)"; Bedrock/Vertex/Foundry alias notes → re-confirm current resolution.
  - **L777**: "`xhigh` on Opus 4.7, `high` on Opus 4.6/Sonnet 4.6" → update tiering.
  - **L1175**: `sonnet[1m]` / `opus[1m]` 1M-context row — confirm Opus 4.8 1M is now native (this session is `claude-opus-4-8[1m]`).
  - **L1322–1328**: alias-resolution + 1M-context-per-provider notes "May 2026: `opus` = Opus 4.7 …" → refresh for 4.8.
  - Add Opus 4.8 + `/fast` (fast mode on 4.8/4.7/4.6) wherever model capabilities are enumerated.
- **Roster note:** `claude-master` is NOT a tracked entry in `roster.json` `agents{}` (it's a plugin/installed agent; appears only in `_schema_pilot_v4_4` metadata). So there's no roster `last_upgraded` to bump — the action is editing the `.md` (and optionally adding a roster entry). Don't expect `/prism-roster` to show it.
- **Suggested approach:** have the updated/working `claude-master` (or `claude-code-guide`) research current Opus 4.8 capabilities from code.claude.com, then apply edits with citations. This is also a good smoke-test that the agent works post-update.

## 4. The three reviews (TASK 2) — scope + focus
Run with the updated `claude-master` (and/or the bundled `/code-review`, `/security-review` skills). Suggested scope: the v5.0 diff vs `main` (`git diff main...feat/v5.0`) — i.e. F4 Phases A–E + the release cut + the routing fix.

**Codebase review** — new/changed modules: `tools/lib/prism-bm25.mjs`, `tools/prism-kb-knowledge-indexer.mjs`, `tools/prism-kb-knowledge-rebuild.mjs`, `tools/lib/prism-kb-knowledge-query.mjs`, `tools/lib/prism-kb-rerank.mjs`, `hooks/prism-kb-autosync.mjs`, `hooks/lib/prism-freshness-sweep.mjs`, `hooks/prism-session-end.mjs`, `hooks/lib/prism-opus-classifier.mjs`, `hooks/prism-prompt-tier-router.mjs`, `tools/prism-recall.mjs`. Focus: correctness, conventions, dead code, error handling, Windows path handling (`fileURLToPath`/`pathToFileURL`, atomic write-then-rename).

**Security review** — the F4 attack surface (design §4 was the controlling constraint):
- Cross-project data-exfil (design Challenge 3): default-deny `.prism-kb-share.json` gate, **descriptor-not-body** storage invariant, secret pre-scan completeness (are the 5 `SECRET_PATTERNS` enough? fail-closed?), provenance tagging.
- `claude -p` egress: only secret-scanned, body-free descriptors leave the machine; confirm no body/secret reaches the re-rank stdin. Subscription-auth channel only, no API key.
- Path traversal / symlink handling in the corpus walk; the verdict-log glob; `spawnSync` arg handling (Windows ENAMETOOLONG → tempfile rule [[feedback-windows-cli-test-args]]).

**Performance review** — BM25 scoring over the global index; indexer walk bounds + `POSTINGS_CAP`/sharding threshold (~2000 docs); the 8 s re-rank timeout (and the measured **~20 s Windows `claude -p` cold-start** → re-rank degrades to BM25 by default — documented limitation, [[feedback-claude-p-cold-start-windows]]); freshness-sweep walk budgets (`budget.n` ceilings).

## 5. Definition of done / delivery (TASKS 3–4)
1. Reviews complete; findings triaged (blocker vs nice-to-have) and blockers fixed (TDD).
2. **Full test sweep re-run + ground-truthed** ([[feedback-subagent-test-count-decay]]). F4 baseline to confirm: **239/239** across the 8 suites in §6 + `test-prism-release-screen-panel` 7/7. Plus drift-guard 3/0. `test-prism-installer.mjs` is flaky — ignore its wobble.
3. Version 5.0.0 consistent (drift-guard green); CHANGELOG `[5.0.0]` final.
4. **Merge `feat/v5.0` → `main`** (open PR: `https://github.com/vosser24/prism_master/pull/new/feat/v5.0` — note `gh` may be at `~/.local/bin`, [[feedback-windows-gh-cli-absent]]), **tag `v5.0.0`**, push tag. This is the human-gated step the user has held to each release.

## 6. Verification commands
```bash
# F4 suites (expect 53/51/56/39/14/13/5/8 = 239) + routing fix (7) + drift (3):
for t in test-prism-bm25 test-prism-kb-knowledge-indexer test-prism-kb-rerank \
         test-prism-recall-cross-project test-prism-kb-knowledge-rebuild \
         test-prism-kb-autosync-knowledge test-prism-freshness-knowledge \
         test-manifest-coverage test-prism-release-screen-panel test-plugin-manifest-drift; do
  node "tests/v3/state/$t.mjs"; done
# Is the v5.0 routing fix live in the running session? (expect the repo=yes / installed=? answer)
grep -c "DESIGN PANEL is decoupled" ~/.claude/hooks/lib/prism-opus-classifier.mjs   # 0 = not installed yet
```

# v4.6 Shipped Handoff — 2026-05-28

Telemetry-driven calibration + data-quality + classifiers + Layer 4 hygiene closeout.

## Ship status

- **Branch:** `feat/v4.6` (pushed to `origin`), 34 commits ahead of `main`.
- **PR:** #5 — https://github.com/vosser24/prism_master/pull/5 (OPEN).
- **Merge + tag `v4.6.0`:** PENDING — deliberately held for human go-ahead (merge-to-main + public tag are irreversible shared-system actions). This is the only remaining step.
- **Version:** bumped to `4.6.0` in `.claude-plugin/plugin.json`, `tools/install-manifest.json` (`prism_version`), and the installer banner. Drift-guard test confirms plugin.json ↔ CHANGELOG `[4.6.0]` consistency.
- **Live install (`~/.claude`):** still effectively v4.5 — `installer update` is version-gated and no-op'd during the build (repo manifest was 4.5.0 until T33). Now that the manifest reads 4.6.0, run `node tools/prism-installer.mjs update && node tools/prism-installer.mjs verify` to push v4.6 hooks/libs live. (Pre-bump, `verify` flagged C1/C2 libs missing from `~/.claude` for exactly this reason — resolves after the update.)

## Ground-truthed test count

**442 assertions across 29 suites, 0 failures — stable across 3 consecutive full runs** (installer run isolated: 90/90). This is the controller-observed count; the handoff's earlier ~426 estimate and one subagent's 391 were both off (subagent test counts are unreliable — counted by the controller per the count-decay lesson).

## What shipped, by layer

**Layer 2 — calibration (headline, recommend-then-apply):**
- `tools/prism-telemetry-aggregate.mjs --recommend-calibration` — reads routing + verdict logs, prints `{knob,current,recommended,evidence,confidence,apply}[]`, persists `~/.claude/.prism-calibration-<date>.json`. Degrades to `insufficient_data` below `MIN_N=15`.
- **Closed-loop (real apply-writers):** K2 escalate-model → `prism-roster --set-model <agent> <model>`; K3 auto-clear → `prism-roster --clear-pending-upgrade <agent>`. Both under `withRosterLock`.
- **Report-only:** K1 cap retune (the parallel cap is orchestrator prose in `dispatch-shapes.md`, not a code knob — D2).
- **Advisory:** K4 master-override gate (`PRISM_OVERRIDE_GATE=strict`); SessionStart can't hard-block (ISSUE-4).
- **Invariant (verified by cross-phase review):** no K-rule mutates a roster/policy default itself — the engine only emits apply-command strings.

**Layer 1 — data-quality:** `dispatch_cap` carries `actual_parallel` + `queue_depth` (mtime-fresh `.prism-task-*` count); `evidence_class` classified at panel-write; `.prism-routing.jsonl` schema **3 → 4 (additive)**.

**Layer 3 — classifiers:** C1 `prism-validation-classify.mjs`, C2 `prism-failure-taxonomy.mjs`, D1 stakes detection in `prism-tier-classify.mjs`.

**Layer 4 — hygiene:** H1 `withRosterLock` ×3 writers; H2 verdict-flag `kind` unification (phase-0d keeps no-jsonl); H3 `--target` hook-path rewrite; H4 Windows `claude` `.cmd` resolution.

## ISSUE-1..5 fixes (all landed, cross-phase-verified)

1. `--agreement` joined a non-existent `phase_1_5_verdict` event → now reads the real verdict JSONL + phase-0d challenges.
2. `.prism-task-*` over-count → mtime freshness filter (10 min) in dispatch-cap.
3. phase-0d verdict write must NOT append jsonl → `writeVerdict('0d', …, {appendLog:false})`.
4. SessionStart can't hard-block (exit-2 ignored) → K4 advisory-only.
5. SessionStart destructively clears verdicts → master writes `.prism-override-pending-<sha>.json` (only place intent exists); SessionStart drains it.

## Notable review catches (this build)

- **Phase 2:** `prism-uninstall-cleanup` reads the roster pre-lock for candidate selection, re-reads under the lock for the write — a benign TOCTOU (delete-by-name is idempotent; the locked write is race-safe). Deliberately accepted; documented in the Phase 2 checkpoint commit.
- **Phase 3:** D1 STAKES_SIGNALS initially over-fired (6/8 benign dev prompts → opus+panel; bare `token`/`migrate`/`billing`/`security`) and missed `credentials` (plural). Re-anchored with context → 0/8 benign FP, 3/3 high-stakes hit. Regression guards added to the classifier test.

## Deviations from the plan worth knowing

- H2: the plan's file list omitted two guard tests that the unified `kind` scheme touches (`test-prism-verdict-log`, `test-prism-phase-0d-oob`) — both updated (verdict `kind` is now `'0d'`/`'1-5'`, not `'phase_0d'`).
- H3: `hookRoot = CLAUDE_DIR` (the plan's `join(CLAUDE_DIR,'hooks')` would double the `hooks/` segment); gate on `flags.target` (the plan's `isDefaultHome` would wrongly rewrite `--home` test installs).
- Phase 4 engine (K0–K4) committed as 3 coherent commits rather than the plan's 5 per-rule commits (single-file cohesive edits).

## Deferred to v4.7

- K1 apply stays report-only (cap is prose, not a code knob).
- `actual_parallel` is a fresh-dir-count proxy, not true sibling state (Claude Code doesn't expose it).
- A dedicated `/security-review` pass (build relied on the 2-lens cross-phase review; v4.6 adds no new network/external-input surface).

## Resume protocol (if picking this up later)

1. `git checkout feat/v4.6`; confirm PR #5 state.
2. If merging: merge PR #5 → `git checkout main && git pull` → `git tag -a v4.6.0 -m "…" && git push origin v4.6.0`.
3. Push v4.6 live: `node tools/prism-installer.mjs update && verify` (now that manifest is 4.6.0).
4. Re-ground-truth before any further claim: `for f in tests/v3/state/test-*.mjs; do node "$f" 2>&1 | tail -1; done` (installer isolated) — expect 442/442.

Links: design `[[2026-05-28-v4.6-design]]`, plan `docs/prism/plans/2026-05-28-v4.6-implementation-plan.md`.

Root-cause Phase 1.5 ETIMEDOUT
Repo: C:\dev\prism_3

TASK: D064 remedy 2 — root-cause why EVERY Phase 1.5 OOB reviewer spawn fails with ETIMEDOUT. This capability has produced ZERO verdicts across its entire lifetime (11 pending-written, 0 verdict-written). Remedies 1, 3, 4 already shipped (commits 8cd07c3b0, 8b459be0b) and made the failure honest and self-reporting; this one is about making it actually WORK.

Read `docs/prism/adjudications/D064-shipped-capability-dead-behind-mislabeled-error-code.md` first for full context.

**REPRODUCE FIRST — this is mandatory, do not skip to a fix.**

1. Read `hooks/prism-phase-1-5-oob.mjs` fully, especially `invokeReviewerInline` and the spawn site (~line 382, a 50000ms spawnSync timeout constant). Determine EXACTLY what argv, cwd, env, stdio and input the hook passes to `claude`.
2. Inspect the 9 orphaned payloads at `~/.claude/.prism-phase-1-5-pending-*.json` — READ them (do NOT delete; they are evidence). How big are they? A very large prompt payload piped to stdin could itself be the problem.
3. Reproduce the spawn MANUALLY with a stopwatch: run the exact same `claude` argv the hook builds, with the same input, and TIME it. Use a bounded timeout (e.g. `timeout 120 ...`) so you never hang. Report wall-clock and exit status.

**HYPOTHESES — test cheapest first, report evidence for/against each:**
(a) Legitimate slowness: `claude -p` cold start plus a large payload over this SMB mount genuinely exceeds 50s. Evidence would be: manual run succeeds but takes >50s.
(b) The child BLOCKS forever rather than running: stdin not closed / no `--print` / it is waiting on interactive auth, a consent prompt, or a TTY. Evidence: manual run hangs indefinitely and produces no output. NOTE this is the highest-prior hypothesis — a hook spawning an interactive CLI non-interactively is a classic hang, and `timeout` masking it as ETIMEDOUT fits the observed data perfectly.
(c) Bad argv/env: the child errors immediately but the error is swallowed. Evidence: fast non-zero exit with stderr.
(d) Recursion/deadlock: `claude` spawned from inside a running Claude Code session contends with a lock or waits on the parent session. Evidence: works standalone from a plain shell, hangs when spawned from the hook.

**Note the ~10.2s SMB hashing figure elsewhere in this repo — I/O over this share is genuinely slow, so do not dismiss (a) without measurement. But equally do not just raise the timeout: if the child blocks forever, a bigger constant changes nothing except how long you wait.**

**THEN, only once the cause is established by measurement:**
- Implement the minimal correct fix. If the cause is a blocking child, fix the invocation (close stdin, add the right non-interactive flags, etc.) rather than the timeout. If it is genuine slowness, raise the constant to a measured value with a comment stating the measurement and date.
- Prove it end-to-end: an actual `verdict-written` event must be observed. Show the log line. Without that proof the fix is unverified — say so plainly rather than claiming success.
- Update `docs/prism/adjudications/D064-...md`: mark remedy 2 with its outcome and record the measurement. Also attempt to resolve the file's open "11 arms vs 10 accounted for" arithmetic gap if your log analysis settles it; if it does not, say so and leave it open.

**HARD CONSTRAINTS:**
- Edit/Write tools ONLY for file changes — never Bash/PowerShell redirection or Set-Content (UTF-8 BOM mangles files).
- Run NO git commands whatsoever, not even read-only status/diff. The parent owns all git. Absolute.
- Do NOT delete or move the 9 orphaned pending payload files.
- Do NOT touch: .claude/agents/MEMORY.md, .claude/references/knowledge-index.md, .prism-task-a2017fb3/, tests/v3/dispatch-preamble.test.mjs (ring-fenced test 6e), or anything under docs/ OTHER than the D064 file.
- NEVER run an unbounded long command. Always wrap in `timeout N`. If something hangs, that is a FINDING — report it, do not wait on it.
- If you conclude the honest answer is "the mechanism cannot work as designed here", say that. A well-evidenced negative result is a valid and valuable outcome; do not manufacture a fix.

VERIFY and paste raw output: `node --check` on any .mjs you edit, plus `node tests/v3/state/test-prism-phase-1-5-oob.mjs` (expect 8/8) and `node tests/v3/state/test-prism-state.mjs` (expect 58/0). Glob for the exact paths if they differ.

Report: the measured reproduction (argv, timing, exit status), evidence for/against each hypothesis, the established root cause, your fix, and the end-to-end proof — or an explicit statement that proof was not obtained.